@page "/Setting"
<MudContainer MaxWidth="MaxWidth.Medium">
    <MudSwitch T="bool" CheckedChanged="AutoStartCheckChanged" Label="开机启动"></MudSwitch>
</MudContainer>

@code {
    bool _autoStart { get; set; } = false;

    private void AutoStartCheckChanged(bool autoStart)
    {
        if (OperatingSystem.IsWindows())
        {
            string StartupPath = Environment.GetFolderPath(Environment.SpecialFolder.CommonStartup);
            string CurrentDir = Environment.CurrentDirectory;
            string exeDir = Path.Join(CurrentDir, "ClashCs.exe.lnk");
        }else if(OperatingSystem.IsLinux()){
            
        }

    }
}